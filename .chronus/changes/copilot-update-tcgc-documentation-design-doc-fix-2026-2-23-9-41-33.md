---
changeKind: fix
packages:
  - "@azure-tools/typespec-client-generator-core"
---

Fix incorrect numeric values for `InitializedBy.parent` and `InitializedBy.individually` in the client design document (values were swapped).
