---
changeKind: fix
packages:
  - "@azure-tools/typespec-client-generator-core"
---

Fix doc comment typos and inaccuracies: corrected garbled `@override` example comment, fixed `@paramAlias` example to use correct `{parameters: ...}` wrapper format, and fixed "operatio" typo in `@markAsLro` doc comment. Updated guideline.md to replace deprecated `@moveTo` references with `@clientLocation`.
