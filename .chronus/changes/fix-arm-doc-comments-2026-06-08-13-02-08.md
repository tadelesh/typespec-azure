---
changeKind: fix
packages:
  - "@azure-tools/typespec-azure-resource-manager"
---

Fix doc comment typos and inaccuracies: correct "patched" to "listed" in list operation templates, fix typo in `@armLibraryNamespace` description, remove ghost `@param`/`@template` tags, and fix broken `@doc` template syntax in private endpoints
