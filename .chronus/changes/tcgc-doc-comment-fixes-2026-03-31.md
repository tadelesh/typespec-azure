---
changeKind: fix
packages:
  - "@azure-tools/typespec-client-generator-core"
---

Fix doc comment issues: garbled text in `@override` example, incorrect `@paramAlias` example missing `{parameters: ...}` wrapper, and typo "operatio" in `@markAsLro` doc
