---
changeKind: feature
packages:
  - "@azure-tools/azure-http-specs"
---

Add Spector test spec for `@clientApiVersions` decorator to cover extending the client API version enum beyond what the service defines.
