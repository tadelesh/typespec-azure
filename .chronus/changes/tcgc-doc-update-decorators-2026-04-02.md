---
changeKind: fix
packages:
  - "@azure-tools/typespec-client-generator-core"
---

Fix doc comments: typo in `@markAsLro`, add `@operationGroup` deprecation notice to `@client`, add `InitializedBy.customizeCode` to `@clientInitialization`, add `Operation` target to `@clientLocation`, and update `@access` target description.
